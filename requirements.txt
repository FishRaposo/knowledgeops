# Aggregate runtime + test dependencies across all KnowledgeOps Python services.
# shared-core and the local `knowledgeops-shared` package are installed separately
# by `make install` (editable). The web-app and llm-gateway (TypeScript) manage
# their own dependencies via npm.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.19.0
redis>=5.0.0
httpx>=0.26.0
python-multipart>=0.0.6
PyMuPDF>=1.23.0
markdown-it-py>=3.0.0
beautifulsoup4>=4.12.0
python-docx>=1.1.0
numpy>=1.26.0
pyyaml>=6.0.1
arq>=0.26.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
pydantic>=2.5.0
pydantic-settings>=2.1.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
ruff>=0.1.0
