# KnowledgeOps Architecture (C4 Model)

## Context Diagram (C4 Level 1)

```mermaid
graph TB
    Users[Users<br/>Internal Teams]
    KnowledgeOps[KnowledgeOps Platform]
    Documents[(Document Sources<br/>PDFs, Wikis, etc.)]
    LLMProviders[LLM Providers<br/>OpenAI, etc.]

    Users -->|Upload/search/query| KnowledgeOps
    KnowledgeOps -->|Ingest| Documents
    KnowledgeOps -->|Generate| LLMProviders
```

## Container Diagram (C4 Level 2)

```mermaid
graph TB
    subgraph Platform[KnowledgeOps Platform]
        WebApp[Web App<br/>Next.js :3000]
        APIGateway[API Gateway<br/>FastAPI :8000]
        Nginx[Nginx Reverse Proxy<br/>:80]

        subgraph Services[Microservices]
            Auth[Auth Service<br/>JWT, RBAC]
            Ingestion[Ingestion Service<br/>Parsing, Chunking]
            Retrieval[Retrieval Service<br/>Search, Rerank]
            Eval[Eval Service<br/>Testing, Judges]
            LLMGateway[LLM Gateway<br/>Proxy, Cache]
            Trace[Trace Service<br/>Observability]
        end

        PostgreSQL[(PostgreSQL<br/>pgvector)]
        Redis[(Redis<br/>Cache, Queue)]
    end

    Users -->|HTTPS| Nginx
    Nginx -->|/| WebApp
    Nginx -->|/api| APIGateway
    APIGateway --> Auth
    APIGateway --> Ingestion
    APIGateway --> Retrieval
    APIGateway --> Eval
    APIGateway --> LLMGateway
    APIGateway --> Trace
    Ingestion -->|Queue| Redis
    Ingestion -->|Store| PostgreSQL
    Retrieval -->|Query| PostgreSQL
    LLMGateway -->|Cache| Redis
```

## Component Diagram (C4 Level 3) - Ingestion Pipeline

```mermaid
graph TB
    subgraph IngestionService[Ingestion Service]
        API[API Layer<br/>Upload endpoints]
        Queue[Redis Queue<br/>Async processing]

        subgraph Workers[Worker Pool]
            Parser[Parser<br/>PDF, MD, DOCX]
            Chunker[Chunker<br/>Text segmentation]
            Embedder[Embedder<br/>OpenAI embeddings]
            Deduplicator[Deduplicator<br/>SHA-256 hashes]
        end

        Status[Status Tracker<br/>Processing state]
    end

    Database[(PostgreSQL)]
    Storage[(Object Storage<br/>S3/Local)]

    API -->|Enqueue| Queue
    Queue -->|Dequeue| Parser
    Parser --> Chunker --> Embedder --> Deduplicator
    Deduplicator -->|Store chunks| Database
    Parser -->|Store raw| Storage
    Deduplicator -->|Update| Status
```

## Data Flow - Document Ingestion

```mermaid
sequenceDiagram
    participant User as User
    participant API as API Gateway
    participant Ingest as Ingestion Service
    participant Queue as Redis Queue
    participant Worker as Worker
    participant Parser as Parser
    participant DB as PostgreSQL

    User->>API: POST /api/documents
    API->>Ingest: Forward upload
    Ingest->>Storage: Store file
    Ingest->>Queue: Enqueue job
    Ingest-->>API: Job ID
    API-->>User: 202 Accepted

    Worker->>Queue: Poll for job
    Queue-->>Worker: Job data

    Worker->>Parser: Parse document
    Parser-->>Worker: Extracted text

    Worker->>Worker: Chunk text
    Worker->>Worker: Generate embeddings
    Worker->>Worker: Deduplicate (SHA-256)

    Worker->>DB: Store chunks + embeddings
    Worker->>Ingest: Update status
    Ingest->>User: Webhook notification
```

## Service Communication Patterns

| Pattern | Services | Technology |
|---------|----------|------------|
| Synchronous REST | API Gateway ↔ All services | HTTP/JSON |
| Async Queue | Ingestion → Workers | Redis Queue |
| Pub/Sub | Status updates | Redis Pub/Sub |
| Shared DB | All services | PostgreSQL |
| Shared Cache | LLM Gateway, Retrieval | Redis |

## Technology Stack

| Layer | Technology |
|-------|------------|
| Frontend | Next.js 14, React, TypeScript |
| API Gateway | FastAPI, Pydantic |
| Services | FastAPI (Python), Express (Node) |
| Database | PostgreSQL 16, pgvector |
| Cache/Queue | Redis 7 |
| Proxy | Nginx |
| Deployment | Docker Compose |

## Scalability Strategy

| Component | Scaling Approach |
|-----------|------------------|
| Web App | Static export + CDN |
| API Gateway | Horizontal (stateless) |
| Ingestion Workers | Horizontal (queue-based) |
| Retrieval | Read replicas |
| PostgreSQL | Primary + replicas |
| Redis | Cluster mode |

## Service Responsibilities

| Service | Responsibility | Key Endpoints |
|---------|----------------|---------------|
| Auth | Authentication, RBAC | /auth/* |
| Ingestion | Document processing | /ingestion/upload |
| Retrieval | Search, rerank | /retrieval/query |
| Eval | Automated testing | /eval/run |
| LLM Gateway | Provider routing | /llm/* |
| Trace | Observability | /traces/* |
